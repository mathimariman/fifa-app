import { PoolfilterPipe } from './poolfilter.pipe';

describe('PoolfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PoolfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
